buildscript {
    ext {
        compileSdkVer = 28
        buildToolsVer = '29.0.2'
        minSdkVer = 17
        targetSdkVer = 28

        gradleVer = '4.0.0-alpha05'

        kotlinVer = '1.3.61'
    }

    repositories {
        jcenter()
        mavenCentral()
        google()
    }

    dependencies {
        classpath "com.android.tools.build:gradle:${gradleVer}"
        classpath "org.jetbrains.kotlin:kotlin-gradle-plugin:${kotlinVer}"
    }
}

allprojects {
    repositories {
        google()
        jcenter()
        mavenCentral()
        maven { url "https://jitpack.io" }
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}